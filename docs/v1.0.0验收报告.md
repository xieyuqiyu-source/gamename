# 霓虹破界：Neon Breaker v1.0.0 验收报告

验收日期：2026-07-20
阶段名称：正式发行
版本号：`v1.0.0`
总体里程碑：`10 / 10`，`100%`

## 1. 阶段结论

`v1.0.0` 冻结并发布“霓虹破界：Neon Breaker”的完整单机玩法。正式版包含四章 20 关、5 种正面道具、7 类永久升级、4 个独立 Boss、无尽磁域、桌面与触摸双端输入，以及可迁移和异常恢复的 SAVE V3 本地档案。

本阶段不新增会改变平衡的玩法机制，工作集中在正式品牌、便携静态部署、发行级目标总审计、最终双端视觉验收和中文交付文档。

## 2. 正式版交付

- 正式中文名：霓虹破界
- 正式英文名：Neon Breaker
- 原创 SVG 图标：霓虹核心、小球与挡板
- Web App Manifest、浏览器标题、描述、主题色与分享元信息
- Vue 3 + Pinia + Vite + Canvas 2D 纯前端生产包
- 相对基础路径，可部署到域名根目录或子目录
- 中文 README、完整开发文档、发行说明与部署说明

## 3. 目标范围总审计

执行：

```bash
pnpm test:scope
```

`verifyReleaseScope.mjs` 共执行 21 项直接断言，覆盖：

| 目标 | 审计证据 |
| --- | --- |
| 20 个主线关卡 | 4 章，每章 5 关，编号连续 1～20 |
| 4 个独立 Boss | 第 5、10、15、20 关，四个不同代号与机制配置 |
| 5 种正面道具 | 多球、扩展、穿透、减速、双轨激光完整注册 |
| 晶币与永久成长 | 7 类升级，保证收入 2743 覆盖总成本 2720 |
| 新档完整进度 | 顺序结算 20 次三星胜利，得到 20 条记录和 60 星 |
| 无尽模式 | 第 10 关后解锁，波次生成、纪录结算和高耐久砖断言 |
| 实体与特效预算 | 同屏小球 12、掉落 28，并保留三档视觉硬预算 |
| SAVE V3 | 新档、正式版本号、测试接口和本地持久化入口 |
| 双端与部署 | 响应式/安全区样式、正式资源、Manifest 与相对 base |

结果：`21 / 21` 通过。

## 4. 自动化门禁

正式发布命令：

```bash
pnpm test:release
```

门禁顺序：

1. 正式目标范围总审计。
2. Node 单元与集成测试。
3. 20 关平衡、经济与三档性能预算。
4. 第一章第 1～5 关真实物理通关。
5. 第二章第 6～10 关与磁暴主机真实物理通关。
6. 第三、四章第 11～20 关与两个终章 Boss 真实物理通关。
7. 无尽模式连续五波回归。
8. Vite 生产构建。
9. 构建后的嵌套子目录与 390/320 像素生产浏览器门禁。

连续执行结果：所有命令退出码为 0，无跳过项。

## 5. 官方网页游戏客户端

### 5.1 开发版

- 正式品牌后的第 1 关连续推进 3 轮。
- Canvas、`render_game_to_text`、固定时间步和 SAVE V3 摘要一致。
- 砖块、球路、挡板和霓虹反馈均清晰，无错误文件。

### 5.2 终局 Boss

- 第 20 关高画质预览连续推进 3 轮。
- 文本状态持续报告 `ZENITH SINGULARITY`、四阶段上限、两座 4 点模块、41 个首阶段护盾节点和 HIGH 600 粒子预算。
- 最终截图可识别普通砖、2/3 点强化砖、橙色熔芯、紫色护盾、核心、模块、小球拖尾和挡板。
- 三轮输出无 `errors-*.json`。

### 5.3 生产版

- `pnpm build` 后通过本地生产预览读取标题页。
- `render_game_to_text` 可用，初始模式、SAVE V3 新档摘要和 UI 状态正常。
- 入口、SVG 图标、Manifest 和哈希 JavaScript 均返回 HTTP 200。

## 6. 用户当前 Chrome 验收

| 视口 | 页面/状态 | 结果 |
| --- | --- | --- |
| 1300×803 | 标题、战役、强化、设置 | 桌面三栏与全部全局导航可用，无横向溢出 |
| 390×844 | 标题、设置、第 20 关 Boss | 竖屏战场单屏完整；四档设置卡片全部可见 |
| 360×800 | 标题、设置 | 导航触控尺寸、Canvas 和主按钮完整 |
| 320×568 | 标题、第 20 关 Boss | 最窄游戏视口无横向溢出，发球按钮完整 |
| 320×996 | 设置 | 允许纵向滚动，四档卡片均位于可视宽度内 |

最终 Chrome 控制台 warning/error 为 0；临时视口已恢复，标签页留在正式标题页。

## 7. 用户正式档案保护

开发预览使用隔离档案，不执行正式结算。最终刷新核对后，用户档案保持：

- 晶币：151
- 星数：2
- 最高分：14471
- 最佳连击：29
- 第 1 关挑战/通关：1 / 1
- 仅解锁第 2 关
- 无尽模式：锁定
- 画质：`HIGH`
- 画面震动：`ON`
- 减弱闪光：`OFF`

## 8. 最终发布门禁

一次完整、连续的 `pnpm test:release` 得到：

- 正式范围：`21 / 21`，发行文档与品牌资产 `8 / 8`。
- Node 测试：`55 / 55`，失败、跳过、取消均为 0。
- 平衡：保证收入 2743、全部升级成本 2720、余量 23。
- 性能：HIGH/MEDIUM/LOW 的粒子、拖尾、冲击波和浮分均未超过硬预算。
- 第一章：第 1～5 关 `5 / 5` 胜利，第 5 关经过三阶段且核心归零。
- 第二章：第 6～10 关 `5 / 5` 胜利，第 10 关双模块全毁且核心归零。
- 第三、四章：第 11～20 关 `10 / 10` 胜利；第 15 关经过三阶段，第 20 关经过四阶段、双模块全毁、37 枚弹幕、11 次招架并以 1 条生命完成。
- 无尽：连续清除 5 波，进入第 6 波，保留 3 条生命，最高连击 143。
- 构建：CSS `40.98 kB`（gzip `8.73 kB`），JavaScript `187.80 kB`（gzip `63.30 kB`）。
- 生产包：`12 / 12` 通过；四个入口资源均为相对路径并存在，嵌套 `/games/neon-breaker/` 全部 HTTP 200，390/320 像素无横向溢出且四张画质卡片完整，控制台/运行错误为 0。

独立代码审查共发现并修复两个 Important：

1. 范围脚本未把本验收报告列为必需交付物；修复后 `requiredFiles` 包含本报告，fresh 范围检查显示文档与品牌资产 `8 / 8`。
2. 初版只以源码字符串判断相对 base 和响应式样式，可能产生假阳性；现新增 `verifyProductionBundle.mjs`，在 build 后使用 Playwright 真实验证嵌套部署、生产资源、390/320 布局与浏览器错误。

最终只读复审结论：Critical 0、Important 0、Minor 0，可以发布。

## 9. 完成定义核对

- [x] 正式名称、原创图标和发行元信息
- [x] 四章 20 关、5 道具、7 升级、4 Boss 与无尽总审计
- [x] 新档顺序 20 次三星结算与 60 星状态
- [x] SAVE V3 兼容与用户正式档案隔离
- [x] 官方客户端开发版、终局 Boss 与生产版检查
- [x] 桌面、390、360、320 像素 Chrome 视觉验收
- [x] 控制台 0 warning/error
- [x] 中文 README、主开发文档、发行说明与部署说明
- [x] 最终连续发行门禁与两个 Important 审查项修复
- [x] 最终定向复审与差异检查
- [ ] Git 提交、标签与远程推送

## 10. 后续维护

`v1.0.0` 完成后，当前玩法目标视为已交付。后续新增关卡、音频、云存档或联网功能均应作为新的维护/功能版本另行规划，不影响本正式版完成状态。
